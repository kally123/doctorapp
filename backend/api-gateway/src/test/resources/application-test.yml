spring:
  application:
    name: api-gateway-test

  cloud:
    gateway:
      routes: []

  data:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    root: WARN
    com.healthapp: INFO

